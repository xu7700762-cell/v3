[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vestibular-fusion"
version = "27.0.0"
description = "VestibularFusion v27 PolynomialKAN reproducibility package"
requires-python = ">=3.10,<3.11"
dependencies = [
  "numpy==2.2.6",
  "scipy==1.15.3",
  "scikit-learn==1.7.2",
  "openpyxl==3.1.5",
  "joblib==1.5.3",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
