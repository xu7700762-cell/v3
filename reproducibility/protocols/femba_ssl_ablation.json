{
  "schema": "femba_ssl_linear_probe_v1",
  "training_seed_default": 2001,
  "split_seed": 42,
  "outer_folds": 5,
  "encoder": "TemporalEncoder_native_four_block_bidirectional",
  "input_shape": [30, 1280],
  "token_shape": [80, 525],
  "pooling": "token_mean_then_task_window_mean",
  "head": "Linear(525,1,bias=True)",
  "state_input": "single_window_excluding_U3_U6",
  "severity_input": "locked_uniform_11_windows_no_context_no_jitter",
  "preprocessing": "existing_offline_transductive_subject_EA",
  "training_input": "online_raw_windows_no_feature_cache",
  "optimizer": "AdamW",
  "head_lr": 0.001,
  "encoder_lr": 0.0001,
  "weight_decay": 0.01,
  "grad_clip": 1.0,
  "precision": "cuda_bfloat16_autocast_fp32_pooling_and_loss",
  "batch_size": {"state": 32, "severity": 4},
  "loss": "BCEWithLogitsLoss",
  "pos_weight": "current_training_partition_negative_count_over_positive_count",
  "label_smoothing": 0.0,
  "sampling": "all_examples_once_per_epoch_paired_shuffle",
  "selection": "minimum_unweighted_validation_BCE_earliest_tie",
  "min_epochs": 20,
  "max_epochs": 60,
  "patience": 15,
  "refit": "reset_to_original_initialization_then_all_source_for_selected_epochs",
  "threshold": 0.5,
  "limits": [
    "Single training seed and fixed optimization budget; no claim of optimal hyperparameters.",
    "All variants use FEMBA; this experiment cannot establish superiority over other architectures.",
    "Existing EA uses all unlabeled windows within each subject, including held-out subjects.",
    "Local pretraining source scripts include guoshanche and monifeixing. The original sample manifest is missing, so monifeixing pretraining overlap is not excluded and strict unseen-data transfer is not established.",
    "The official checkpoint is a restored encoder-only artifact; dataset identity overlap across collections has not been independently verified."
  ]
}
